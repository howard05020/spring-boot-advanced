spring:
  thymeleaf:
     mode: HTML
     
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_advanced?useUnicode=true&characterEncoding=utf-8
    username: root
    password: howard30039593
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

logging:
  level:
    root: info
    com.howard: debug
  file:
    name: logs/debug.log
